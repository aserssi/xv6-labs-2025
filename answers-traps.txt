1. Backtrace通过读取s0帧指针遍历内核栈。返回地址位于fp-8，上一层帧指针位于fp-16。

2. sigalarm记录报警间隔和用户处理函数地址。时钟中断达到指定次数后，内核保存完整trapframe，并将epc改为处理函数地址。

3. sigreturn恢复之前保存的trapframe，从而恢复所有用户寄存器和原程序执行位置。为保持a0不变，系统调用返回保存的a0值。

4. alarm_active用于阻止报警处理函数执行期间再次进入处理函数。
